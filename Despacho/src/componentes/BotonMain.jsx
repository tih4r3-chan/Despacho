import React from 'react';

function BotonMain({className }) {
  return (
    <button
      className={`"px-16 bg-teal-500/80 border rounded-xl font-bold hover:bg-teal-600 transition-all duration-500 text-white">${className}`}>
      ayuda xfavorx
    </button>
  );
}

export default BotonMain;
